# Actuator Demo

This example demonstrates using Spring Boot Actuator in a simple web application.

## How to run

```bash
mvn spring-boot:run
```

The application exposes a `/hello` endpoint and allows access to Actuator `health` and `info` endpoints.
